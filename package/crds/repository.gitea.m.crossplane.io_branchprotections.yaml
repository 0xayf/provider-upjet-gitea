---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: branchprotections.repository.gitea.m.crossplane.io
spec:
  group: repository.gitea.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitea
    kind: BranchProtection
    listKind: BranchProtectionList
    plural: branchprotections
    singular: branchprotection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BranchProtection is the Schema for the BranchProtections API.
          <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BranchProtectionSpec defines the desired state of BranchProtection
            properties:
              forProvider:
                properties:
                  approvalWhitelistTeams:
                    description: |-
                      Only reviews from allowlisted teams will count to the required
                      approvals. Without approval allowlist, reviews from anyone with
                      write access count to the required approvals.
                    items:
                      type: string
                    type: array
                  approvalWhitelistUsers:
                    description: |-
                      Only reviews from allowlisted users will count to the required
                      approvals. Without approval allowlist, reviews from anyone with
                      write access count to the required approvals.
                    items:
                      type: string
                    type: array
                  blockMergeOnOfficialReviewRequests:
                    description: |-
                      Merging will not be possible when it has official
                      review requests, even if there are enough approvals.
                    type: boolean
                  blockMergeOnOutdatedBranch:
                    description: Merging will not be possible when head branch is
                      behind base branch.
                    type: boolean
                  blockMergeOnRejectedReviews:
                    description: |-
                      Merging will not be possible when changes are
                      requested by official reviewers, even if there are enough
                      approvals.
                    type: boolean
                  dismissStaleApprovals:
                    description: |-
                      When new commits that change the content of the pull request
                      are pushed to the branch, old approvals will be dismissed.
                    type: boolean
                  enablePush:
                    description: |-
                      Anyone with write access will be allowed to push to this branch
                      (but not force push), add a whitelist users or teams to limit
                      access.
                    type: boolean
                  mergeWhitelistTeams:
                    description: Allow only allowlisted teams to merge pull requests
                      into this branch.
                    items:
                      type: string
                    type: array
                  mergeWhitelistUsers:
                    description: Allow only allowlisted users to merge pull requests
                      into this branch.
                    items:
                      type: string
                    type: array
                  name:
                    description: Repository name
                    type: string
                  protectedFilePatterns:
                    description: Protected file patterns (separated using semicolon
                      ';')
                    type: string
                  pushWhitelistDeployKeys:
                    description: Allow deploy keys with write access to push. Requires
                      enable_push to be set to true.
                    type: boolean
                  pushWhitelistTeams:
                    description: Allowlisted teams for pushing. Requires enable_push
                      to be set to true.
                    items:
                      type: string
                    type: array
                  pushWhitelistUsers:
                    description: Allowlisted users for pushing. Requires enable_push
                      to be set to true.
                    items:
                      type: string
                    type: array
                  requireSignedCommits:
                    description: Reject pushes to this branch if they are unsigned
                      or unverifiable.
                    type: boolean
                  requiredApprovals:
                    description: Allow only to merge pull request with enough positive
                      reviews.
                    type: number
                  ruleName:
                    description: Protected Branch Name Pattern
                    type: string
                  statusCheckPatterns:
                    description: |-
                      Enter patterns to specify which status checks must pass before
                      branches can be merged into a branch that matches this rule.
                      Each line specifies a pattern. Patterns cannot be empty.
                    items:
                      type: string
                    type: array
                  unprotectedFilePatterns:
                    description: Unprotected file patterns (separated using semicolon
                      ';')
                    type: string
                  username:
                    description: User name or organization name
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  approvalWhitelistTeams:
                    description: |-
                      Only reviews from allowlisted teams will count to the required
                      approvals. Without approval allowlist, reviews from anyone with
                      write access count to the required approvals.
                    items:
                      type: string
                    type: array
                  approvalWhitelistUsers:
                    description: |-
                      Only reviews from allowlisted users will count to the required
                      approvals. Without approval allowlist, reviews from anyone with
                      write access count to the required approvals.
                    items:
                      type: string
                    type: array
                  blockMergeOnOfficialReviewRequests:
                    description: |-
                      Merging will not be possible when it has official
                      review requests, even if there are enough approvals.
                    type: boolean
                  blockMergeOnOutdatedBranch:
                    description: Merging will not be possible when head branch is
                      behind base branch.
                    type: boolean
                  blockMergeOnRejectedReviews:
                    description: |-
                      Merging will not be possible when changes are
                      requested by official reviewers, even if there are enough
                      approvals.
                    type: boolean
                  dismissStaleApprovals:
                    description: |-
                      When new commits that change the content of the pull request
                      are pushed to the branch, old approvals will be dismissed.
                    type: boolean
                  enablePush:
                    description: |-
                      Anyone with write access will be allowed to push to this branch
                      (but not force push), add a whitelist users or teams to limit
                      access.
                    type: boolean
                  mergeWhitelistTeams:
                    description: Allow only allowlisted teams to merge pull requests
                      into this branch.
                    items:
                      type: string
                    type: array
                  mergeWhitelistUsers:
                    description: Allow only allowlisted users to merge pull requests
                      into this branch.
                    items:
                      type: string
                    type: array
                  name:
                    description: Repository name
                    type: string
                  protectedFilePatterns:
                    description: Protected file patterns (separated using semicolon
                      ';')
                    type: string
                  pushWhitelistDeployKeys:
                    description: Allow deploy keys with write access to push. Requires
                      enable_push to be set to true.
                    type: boolean
                  pushWhitelistTeams:
                    description: Allowlisted teams for pushing. Requires enable_push
                      to be set to true.
                    items:
                      type: string
                    type: array
                  pushWhitelistUsers:
                    description: Allowlisted users for pushing. Requires enable_push
                      to be set to true.
                    items:
                      type: string
                    type: array
                  requireSignedCommits:
                    description: Reject pushes to this branch if they are unsigned
                      or unverifiable.
                    type: boolean
                  requiredApprovals:
                    description: Allow only to merge pull request with enough positive
                      reviews.
                    type: number
                  ruleName:
                    description: Protected Branch Name Pattern
                    type: string
                  statusCheckPatterns:
                    description: |-
                      Enter patterns to specify which status checks must pass before
                      branches can be merged into a branch that matches this rule.
                      Each line specifies a pattern. Patterns cannot be empty.
                    items:
                      type: string
                    type: array
                  unprotectedFilePatterns:
                    description: Unprotected file patterns (separated using semicolon
                      ';')
                    type: string
                  username:
                    description: User name or organization name
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.ruleName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ruleName)
                || (has(self.initProvider) && has(self.initProvider.ruleName))'
            - message: spec.forProvider.username is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.username)
                || (has(self.initProvider) && has(self.initProvider.username))'
          status:
            description: BranchProtectionStatus defines the observed state of BranchProtection.
            properties:
              atProvider:
                properties:
                  approvalWhitelistTeams:
                    description: |-
                      Only reviews from allowlisted teams will count to the required
                      approvals. Without approval allowlist, reviews from anyone with
                      write access count to the required approvals.
                    items:
                      type: string
                    type: array
                  approvalWhitelistUsers:
                    description: |-
                      Only reviews from allowlisted users will count to the required
                      approvals. Without approval allowlist, reviews from anyone with
                      write access count to the required approvals.
                    items:
                      type: string
                    type: array
                  blockMergeOnOfficialReviewRequests:
                    description: |-
                      Merging will not be possible when it has official
                      review requests, even if there are enough approvals.
                    type: boolean
                  blockMergeOnOutdatedBranch:
                    description: Merging will not be possible when head branch is
                      behind base branch.
                    type: boolean
                  blockMergeOnRejectedReviews:
                    description: |-
                      Merging will not be possible when changes are
                      requested by official reviewers, even if there are enough
                      approvals.
                    type: boolean
                  createdAt:
                    description: Webhook creation timestamp
                    type: string
                  dismissStaleApprovals:
                    description: |-
                      When new commits that change the content of the pull request
                      are pushed to the branch, old approvals will be dismissed.
                    type: boolean
                  enableApprovalWhitelist:
                    description: True if a approval whitelist is used.
                    type: boolean
                  enableMergeWhitelist:
                    description: True if a merge whitelist is used.
                    type: boolean
                  enablePush:
                    description: |-
                      Anyone with write access will be allowed to push to this branch
                      (but not force push), add a whitelist users or teams to limit
                      access.
                    type: boolean
                  enablePushWhitelist:
                    description: True if a push whitelist is used.
                    type: boolean
                  enableStatusCheck:
                    description: |-
                      Require status checks to pass before merging. When enabled,
                      commits must first be pushed to another branch, then merged
                      or pushed directly to a branch that matches this rule after
                      status checks have passed. If no contexts are matched, the
                      last commit must be successful regardless of context
                    type: boolean
                  id:
                    type: string
                  mergeWhitelistTeams:
                    description: Allow only allowlisted teams to merge pull requests
                      into this branch.
                    items:
                      type: string
                    type: array
                  mergeWhitelistUsers:
                    description: Allow only allowlisted users to merge pull requests
                      into this branch.
                    items:
                      type: string
                    type: array
                  name:
                    description: Repository name
                    type: string
                  protectedFilePatterns:
                    description: Protected file patterns (separated using semicolon
                      ';')
                    type: string
                  pushWhitelistDeployKeys:
                    description: Allow deploy keys with write access to push. Requires
                      enable_push to be set to true.
                    type: boolean
                  pushWhitelistTeams:
                    description: Allowlisted teams for pushing. Requires enable_push
                      to be set to true.
                    items:
                      type: string
                    type: array
                  pushWhitelistUsers:
                    description: Allowlisted users for pushing. Requires enable_push
                      to be set to true.
                    items:
                      type: string
                    type: array
                  requireSignedCommits:
                    description: Reject pushes to this branch if they are unsigned
                      or unverifiable.
                    type: boolean
                  requiredApprovals:
                    description: Allow only to merge pull request with enough positive
                      reviews.
                    type: number
                  ruleName:
                    description: Protected Branch Name Pattern
                    type: string
                  statusCheckPatterns:
                    description: |-
                      Enter patterns to specify which status checks must pass before
                      branches can be merged into a branch that matches this rule.
                      Each line specifies a pattern. Patterns cannot be empty.
                    items:
                      type: string
                    type: array
                  unprotectedFilePatterns:
                    description: Unprotected file patterns (separated using semicolon
                      ';')
                    type: string
                  updatedAt:
                    description: Webhook creation timestamp
                    type: string
                  username:
                    description: User name or organization name
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
